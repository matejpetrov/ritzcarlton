<div class="modal-dialog">
	<div class="modal-content">
	  <div class="modal-header">
	    <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
	    <h4 class="modal-title" id="myModalLabel">Delete confirmation</h4>
	  </div>
	  <div class="modal-body">
	    <p>You are about to delete one of your guests, this procedure is irreversible.</p>
        <p>Do you want to proceed?</p>
        <p>Delete <%= @guest.guest_name %> <%= @guest.guest_surname %> ? </p>
	  </div>
	  <div class="modal-footer">       

	  <!-- i had to manually define the path to be the guests/destroy action and the method must be delete.  -->
	  	<%= form_for :guest, url: { controller: "guests", action: "destroy" }, method: :delete, remote: true do |f| %>
	  		<input type="submit" value="Yes" class="btn btn-primary" />
	  	<% end %>

        <button type="button" class="btn btn-default" data-dismiss="modal">No</button>

      </div>	  
	</div>
</div>